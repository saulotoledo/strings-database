spring:
  datasource:
    username: "db_user"
    password: "db_password"
    url: "jdbc:mysql://stringsdb-db:3306/stringsdb"

  jpa:
    show-sql: false
    properties:
      hibernate:
        jdbc:
          time_zone: "UTC"
    hibernate:
      ddl-auto: "update"
